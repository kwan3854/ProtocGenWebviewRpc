// AUTO-GENERATED by protoc-gen-webviewrpc
using System.Threading.Tasks;
using Google.Protobuf;
using WebViewRPC;

namespace {{.CsharpNamespace}}
{
    public class {{.ServiceName}}Client
    {
        private readonly WebViewRpcClient _rpcClient;

        public {{.ServiceName}}Client(WebViewRpcClient rpcClient)
        {
            _rpcClient = rpcClient;
        }

        {{range .Methods}}
        public async Task<{{.OutputType}}> {{.MethodName}}({{.InputType}} request)
        {
            return await _rpcClient.CallMethodAsync<{{.OutputType}}>("{{$.ServiceName}}.{{.MethodName}}", request);
        }
        {{end}}
    }
}
